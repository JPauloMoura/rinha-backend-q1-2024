worker_processes auto;  ## Default: 1

events {
    worker_connections 1300;
}

http {
    proxy_read_timeout 45s;
    proxy_connect_timeout 45s;
    access_log off;
    sendfile   on;

    
    upstream api {
        server api-rinha-01:3001;
        server api-rinha-02:3001;
    }

    server {
        listen 9999;
        
        location / {
            proxy_pass http://api;
        }
    }
}
